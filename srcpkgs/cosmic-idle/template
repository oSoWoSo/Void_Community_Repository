# Template file for 'cosmic-idle'
pkgname=cosmic-idle
version=1.8.0
revision=1
archs="x86_64*"
build_style=cargo
hostmakedepends="pkg-config just"
makedepends="libxkbcommon-devel"
short_desc="Idle daemon for the COSMIC desktop"
maintainer="zenobit <zen@osowoso.org>"
license="GPL-3.0-only"
homepage="https://github.com/pop-os/cosmic-idle"
distfiles="https://github.com/pop-os/cosmic-idle/archive/refs/tags/epoch-${version}.tar.gz"
checksum=b35c98a998687aca66cedbb02d5067c8de4a8340f8c3e12a4b7c44adfa59ef52

do_install() {
	just --set cargo-target-dir "target/${RUST_TARGET}" --set rootdir ${DESTDIR} --set prefix /usr install
}
